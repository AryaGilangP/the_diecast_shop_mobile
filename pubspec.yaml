name: the_diecast_shop_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  pbp_django_auth: ^0.4.0
  http: ^1.2.2
  video_player: ^2.5.1 # Tambahkan dependency ini

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/media/fufufafa-login-bg.mp4 # Tambahkan path video di sini
